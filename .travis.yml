# This file configures Travis CI to test the repo
language: go
  
go:
  - 1.14.x


# Install takes steps to ensure dependencies are in place.
install:
  - go get -t -v


script: 
  - go test -v && go run .